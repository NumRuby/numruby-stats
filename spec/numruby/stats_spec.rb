# frozen_string_literal: true

RSpec.describe Numruby::Stats do
  it "has a version number" do
    expect(Numruby::Stats::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
